# ExamPoll 발표 시나리오

## 순서 요약

| 슬라이드 | 제목 | 발표자 |
|----------|------|--------|
| 1 | 표지 | 이도훈 |
| 2 | 문제 정의 | 이도훈 |
| 3 | 해결 방안 | 이도훈 |
| 4 | 기능 요구사항 | 이도훈 |
| 5 | 비기능 요구사항 | 이도훈 |
| 6 | 시연 | 이동찬 |
| 7 | 기술 스택 | 이동찬 |
| 8 | 역할 분담 | 추은성 |
| 9 | AI 활용 | 추은성 |
| 10 | 입력창 포커스 효과 | 추은성 |
| 11 | 날짜·시간 입력 모듈 | 추은성 |
| 12 | 마무리 | 추은성 |

---

## 이도훈 (슬라이드 1~5)

### [슬라이드 1 — 표지]

> "안녕하세요. 웹프로그래밍 프로젝트 ExamPoll을 발표할 팀장 이도훈입니다. 팀원 추은성, 이동찬과 함께 발표하겠습니다."

---

### [슬라이드 2 — 문제 정의]

> "저희가 이 프로젝트를 기획한 배경입니다. 교수님이 수업 외 시험을 잡으실 때 학생 스케줄을 파악할 마땅한 방법이 없고, 단체 채팅방에서 일일이 확인하는 방식은 학생 수가 많을수록 취합이 어렵습니다. 결국 누군가는 빠질 수밖에 없는 구조입니다."

---

### [슬라이드 3 — 해결 방안]

> "그래서 저희는 이 흐름으로 문제를 해결합니다. 교수가 후보 시간대를 등록하고 링크를 공유하면, 학생은 로그인 없이 불가능한 시간만 체크합니다. 마감 후에는 시스템이 자동으로 가장 많은 학생이 참석 가능한 시간을 추천합니다."

**알아두면 좋은 것**
- 이 서비스는 HTML 파일 하나로 여러 화면을 구현하는 **SPA(Single Page Application)** 방식입니다.
- URL 뒤의 `#login`, `#dashboard`, `#vote/id` 같은 해시값이 바뀔 때마다 JavaScript가 화면을 교체합니다.
- 페이지를 새로 불러오지 않기 때문에 빠르고, 링크만 공유하면 해당 화면으로 바로 이동할 수 있습니다.

---

### [슬라이드 4 — 기능 요구사항]

> "기능은 교수와 학생으로 나뉩니다. 교수는 회원가입과 로그인, 투표 개설, 후보 시간대 등록, 결과 조회까지 할 수 있습니다. 학생은 링크만으로 접속해 이름과 학번만 입력하면 투표할 수 있고, 같은 학번으로 다시 제출하면 기존 투표가 수정됩니다. 마감 후에는 최적 시간이 자동으로 추천됩니다."

**알아두면 좋은 것**
- 교수 로그인은 **세션**을 `localStorage`에 저장하는 방식으로 구현했습니다. 브라우저를 닫아도 로그인 상태가 유지됩니다.
- 학생은 별도 계정 없이 학번을 고유 식별자로 사용합니다. 같은 학번으로 재제출하면 서버에서 기존 투표를 덮어써 **중복 투표를 방지**합니다.
- 비밀번호는 서버에 평문으로 저장하지 않고, 브라우저에서 **SHA-256으로 해싱**한 값만 전송·저장합니다.

---

### [슬라이드 5 — 비기능 요구사항]

> "비기능 요구사항입니다. 한 분반 학생이 동시에 접속해도 처리될 수 있도록 했고, 모바일 반응형 UI를 지원합니다. 학생 로그인과 이메일 인증은 범위에서 제외했고, 대신 이름과 학번만으로 참여하는 방식으로 대체했습니다."

**알아두면 좋은 것**
- **반응형 디자인**은 CSS의 `@media` 쿼리로 구현했습니다. 화면 너비에 따라 레이아웃이 자동으로 바뀝니다.
- **학생 로그인을 제외**한 이유: 학번 인증 시스템 연동이 범위를 벗어나고, 시험 일정 투표는 개인정보 보호 수준이 낮아도 되는 서비스이기 때문입니다.

---

## 이동찬 (슬라이드 6~7)

### [슬라이드 6 — 시연]

> "직접 시연하겠습니다. 서버는 로컬에서 Node.js로 실행하고 ngrok으로 외부 접속 주소를 열었습니다. 이 주소로 어디서든 접속 가능합니다."
>
> "먼저 교수 파트입니다. 회원가입 후 로그인하고, 투표를 개설해 후보 시간대를 등록합니다. 생성된 링크와 QR 코드로 학생들에게 공유하는 방식입니다."
>
> "이제 학생 입장에서 보겠습니다. 방금 공유된 링크로 접속해 이름과 학번을 입력하고, 불가능한 시간대를 선택해 제출합니다. 마감 후 결과 화면에서 최적 시간이 초록색으로 강조됩니다."

**알아두면 좋은 것**

- **ngrok**은 로컬 서버를 인터넷에 임시로 노출시켜주는 터널링 도구입니다. 같은 와이파이가 아니어도 외부에서 접속할 수 있습니다.
- `node server.js`를 실행하면 서버가 ngrok을 자동으로 실행하고, 터미널에 공개 주소를 출력합니다.
- QR 코드는 그 공개 주소 기반으로 생성되며, 학생이 스마트폰 카메라로 찍으면 바로 투표 페이지로 이동합니다.

---

### [슬라이드 7 — 기술 스택]

> "기술 스택입니다. 프론트엔드는 HTML·CSS·Vanilla JS만 사용했고, 백엔드는 Node.js 내장 모듈만 써서 외부 패키지가 없습니다. DB는 JSON 파일로 대체했고, 비밀번호는 Web Crypto API의 SHA-256으로 해싱했습니다. 배포는 ngrok을 이용했고, QR 코드는 qrcodejs CDN으로 클라이언트에서 바로 생성합니다."

**알아두면 좋은 것**
- **Node.js 내장 모듈만 사용**한 이유: `npm install` 없이 `node server.js` 하나로 바로 실행할 수 있도록 하기 위해서입니다. `http`(서버 생성), `fs`(파일 읽기/쓰기), `path`(경로 처리), `url`(URL 파싱) 모듈을 사용했습니다.
- **JSON 파일 DB**: 교수 계정은 `db_professors.json`, 투표는 `db_polls.json`, 투표 내역은 `db_votes.json`에 저장합니다. MySQL 같은 별도 DB 설치 없이 파일 시스템으로 데이터를 관리합니다.
- **REST API**: 서버는 URL과 HTTP 메서드(GET·POST·DELETE) 조합으로 요청을 구분합니다. 예를 들어 `POST /api/polls`는 투표 생성, `DELETE /api/polls/:id`는 투표 삭제입니다.
- **SHA-256**: 비밀번호를 그대로 저장하면 DB가 노출될 때 위험합니다. 해싱은 원본 복원이 불가능한 단방향 암호화로, 저장된 해시값과 로그인 시 입력값의 해시를 비교해 인증합니다.

---

## 추은성 (슬라이드 8~12)

### [슬라이드 8 — 역할 분담]

> "역할 분담입니다. 이도훈이 교수 플로우 전반과 UI를, 저는 학생 플로우와 최적 시간 추천 알고리즘을, 이동찬이 백엔드 서버와 ngrok 인프라를 담당했습니다."

---

### [슬라이드 9 — AI 활용]

> "AI 활용 범위입니다. Claude와 Gemini를 사용했는데, 백엔드 코드 작성과 디자인 리팩토링, QA에 활용했습니다. 화면 개발과 인터랙션, 요구사항 설계와 실제 테스트는 저희가 직접 진행했습니다."

**알아두면 좋은 것**
- AI가 작성한 코드도 직접 이해하고 검토해야 합니다. 어떤 코드가 왜 그렇게 작성됐는지 질문을 받을 수 있으니, 본인 파트 코드는 흐름을 파악해두는 게 좋습니다.
- **최적 시간 추천 알고리즘**은 AI 없이 직접 구현한 부분입니다. 각 시간 슬롯에 대해 불가 응답 수를 카운트하고, 가장 적은 슬롯을 추천합니다. (`getOptimalSlot` 함수, `data.js`)

---

### [슬라이드 10 — 입력창 포커스 효과]

> "수업에서 배운 내용을 적용한 부분입니다. `input:focus` 가상 선택자로 커서가 오면 배경이 흰색으로 바뀌고 파란 테두리와 그림자가 생깁니다. `transition`으로 부드럽게 전환되고, `placeholder`로 아무것도 입력하지 않았을 때 교수 이메일이나 투표 제목 예시를 안내합니다."

**알아두면 좋은 것**
- **가상 선택자(Pseudo-class)**: 요소의 특정 상태를 CSS로 선택하는 문법입니다. `:focus`는 해당 요소에 커서가 올라간 상태, `:hover`는 마우스를 올린 상태를 뜻합니다.
- **가상 요소(Pseudo-element)**: `::placeholder`는 입력창 안의 안내 텍스트를 가리킵니다. `color`와 `opacity`로 스타일을 별도로 지정할 수 있습니다.
- **`transition`**: `transition: all .18s`는 CSS 속성이 변할 때 0.18초 동안 애니메이션을 적용합니다. `all` 대신 `border-color`, `background` 등 특정 속성만 지정할 수도 있습니다.
- **`box-shadow: 0 0 0 3px #EFF6FF`**: X이동·Y이동·블러 없이 3px 두께의 외곽선처럼 보이는 그림자를 만드는 기법입니다. 포커스 링을 커스텀할 때 자주 쓰입니다.

---

### [슬라이드 11 — 날짜·시간 입력 모듈]

> "`datetime-local` 타입으로 날짜와 시간을 한 입력창에서 받습니다. `step=600`으로 10분 간격으로 제한했고, 잘못된 값이 입력되면 `snapTo10Min` 함수가 자동으로 보정합니다. 59분이 들어오면 다음 시각 0분으로 넘어가는 처리도 포함했습니다."

**알아두면 좋은 것**
- **`type="datetime-local"`**: 날짜와 시간을 하나의 입력창에서 받는 HTML5 input 타입입니다. 브라우저가 달력과 시계 UI를 자동으로 제공해 별도 라이브러리가 필요 없습니다.
- **`step` 속성**: 시간 입력의 경우 초 단위로 지정합니다. `step=600`은 600초 = 10분 간격으로 선택지를 제한합니다.
- **`Date` 객체**: JavaScript에서 날짜·시간을 다루는 내장 객체입니다. `new Date(input.value)`로 입력값을 Date로 변환하고, `getMinutes()` / `setMinutes()` / `setHours()`로 분·시간을 읽고 쓸 수 있습니다.
- **보정 로직**: `Math.round(분 / 10) * 10`으로 가장 가까운 10분 단위로 반올림합니다. 결과가 60이면 분을 0으로, 시간을 +1 올려 다음 시각으로 넘깁니다.

---

### [슬라이드 12 — 마무리]

> "이상으로 ExamPoll 발표를 마치겠습니다. 감사합니다."

